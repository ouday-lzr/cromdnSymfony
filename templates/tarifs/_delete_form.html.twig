<form method="post" action="{{ path('tarifs_delete', {'annee': tarif.annee}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tarif.annee) }}">
    <button class="btn">Delete</button>
</form>
